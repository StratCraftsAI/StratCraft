/**
 * Rendering Constants
 *
 * TICKET_476: Magic Number Elimination
 * TICKET_179: Unified Constants Management
 *
 * Chart rendering and worker constants.
 */

// =============================================================================
// Chart Rendering
// =============================================================================

/** Maximum render points for chart downsampling (LTTB) */
export const CHART_MAX_RENDER_POINTS = 2000;

/** Worker render tick interval (ms) */
export const CHART_RENDER_INTERVAL_MS = 100;
