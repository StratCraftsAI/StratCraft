/**
 * TICKET_358: Format utilities for backtest result display
 * TICKET_383: Canonical definitions moved to Tier 0 (data-plugin), re-exported here.
 */

export {
  formatCurrency,
  formatPercent,
  formatRatio,
  formatDate,
  getColorClass,
  safeNum,
} from '@plugins/data-plugin/utils/format-utils';
